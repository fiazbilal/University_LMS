<% name = current_user.present? ? current_user.first_name : 'Guest' %>
<% status = current_user.present? ? current_user.status : 'Student/Teacher' %>

<%if current_user.present? %>
    <%= render partial: 'shared/courseshome'%>
<%else%>
    <h1>Welcome, <%= name %> </h1>
    <h3><%= status%> </h3>
 <% end %>  
